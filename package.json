{
  "name": "realm-drugtrials",
  "version": "0.1.0",
  "private": true,
  "description": "Live, evidence-aware clinical trial intelligence as an Embabel Realm",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.build.json && node -e \"require('fs').writeFileSync('dist/package.json', '{\\\"type\\\":\\\"commonjs\\\"}')\" && embabel-build-manifest",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@embabel/runtime-types": "file:../assistant/tooling/runtime-types",
    "typescript": "^5.4.0",
    "vitest": "^3.2.7"
  }
}
